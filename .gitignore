node_modules/
.next/
.venv/
__pycache__/
.DS_Store
.env
.env.local

# Fremde Git-Repos, die zu einem Projekt gehoeren (projects/README.md).
# Eigene Historie, eigener Ort — nie in das persoenliche Workspace-Repo mitcommitten.
projects/*/code/

# Laufzeit-Artefakte dieses Workspace (kein Versionsgut)
_tmp/*
!_tmp/README.md
context/.backup/
context/.mail_cache.json
context/.triage_ledger.json
context/today.html

# Interne Dateien, gehören nie ins ausgelieferte Repo
AUSLIEFERUNG.md
ROADMAP.md
_abgeloest/

# Lukas Deliverables über das Paket (nie ausliefern) — nur die Wurzel:
# projects/*/outputs/ beim Kunden MUSS mitgesichert werden
/outputs/
